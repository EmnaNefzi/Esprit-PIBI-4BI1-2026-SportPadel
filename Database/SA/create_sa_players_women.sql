CREATE TABLE sa.sa_players_women (
    id BIGSERIAL PRIMARY KEY,
    name          VARCHAR(200),
    country       VARCHAR(100),
    points        VARCHAR(50),
    position      INTEGER,
    move          VARCHAR(50),
    page_no       INTEGER
  );
