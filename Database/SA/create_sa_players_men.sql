CREATE TABLE sa.sa_players_men (
    nome        VARCHAR(255),
    country     VARCHAR(50),
    points      VARCHAR(50),
    position    VARCHAR(50),
    move        VARCHAR(50),
    source_file VARCHAR(255) NOT NULL,
    page_no     INTEGER
);
